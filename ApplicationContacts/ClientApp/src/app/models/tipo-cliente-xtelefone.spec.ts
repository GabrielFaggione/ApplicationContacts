import { TipoClienteXtelefone } from './tipo-cliente-xtelefone';

describe('TipoClienteXtelefone', () => {
  it('should create an instance', () => {
    expect(new TipoClienteXtelefone()).toBeTruthy();
  });
});
